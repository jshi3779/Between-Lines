# Between Lines｜当前设计规范

本文件记录当前已实现的 HTML 原型界面与交互规范。所有尺寸均以 375 × 812px 的手机画板为基准。

## 1. 画板与基础视觉

| 项目 | 规范 |
| --- | --- |
| 画板尺寸 | 375 × 812px |
| 页面底色 | `#ECECEA` |
| 主要文字与图标色 | `#242222` |
| 辅助文字、页码与翻页控件 | `#79746D` |
| 手写字体 | `851 GBai Marker`，缺失时使用手写字体后备字族 |
| 页面素材 | `public/icons/` 内 SVG 资产 |

## 2. 顶部导航

- 顶部导航组距画板上边缘 50px，高 30px。
- 返回、邀请、设置图标均为 30 × 30px，颜色为 `#242222`。
- 返回按钮左侧距画板 20px。
- 邀请与设置间距 10px；设置按钮右侧距画板 20px。
- 标题可直接编辑，字号 20px，与返回按钮保持至少 10px 的水平间距，也与右侧按钮组保持至少 10px 的空间。
- 标题在导航高度内垂直居中；默认标题为 `Untitled`。

## 3. 协作者标签

- 协作者标签组位于标题组下方 15px，即画板顶部 95px。
- 标签组左边缘与返回按钮左边缘对齐，均为 20px。
- 使用 `user-label-1.svg` 至 `user-label-4.svg`，每枚标签 40 × 68px。

## 4. 笔记本页面与翻页

- 笔记本页面从画板顶部 115px 开始，左右边缘贴齐画板。
- 新笔记本默认显示左页；新增页面时按页码交替使用右页、左页。
- 页码导航位于顶部 140px、右侧 20px，使用 `当前页 / 总页数` 格式。
- 上一页与下一页均为 20 × 20px，使用返回图标翻转，支持点击切换页面。
- 右下角的添加页面按钮距右侧 10px、距底部 30px；点击后新增页面并切换为点击状态素材。

## 5. 句子卡片

- 每页最多 5 张句子卡；第 5 张创建后隐藏“添加句子”组件。
- 添加句子组件位于内容区域下方；空页面显示引导文案，创建第一张卡后隐藏。
- 首张卡使用随机示例句；同一笔记本后续新增卡为空白句子。
- 卡片采用固定宽度和最小高度，文字可正常换行；高度仅随文本行数向下增长，不横向撑开。
- 卡片内边距为 16px，头像固定在左上区域且不随卡片高度变化。
- 头像为 4 个随机用户样式之一，保持裁切比例与固定尺寸；文字区域位于头像右侧。
- 当前编辑中的卡片使用白色圆角背景与阴影；其余卡片不显示该激活外框。
- 卡片右下角提供删除整张句子卡的按钮。

## 6. 句子编辑与词卡

- 句子文本可随时点击编辑；长单词、网址和连续字符必须自动换行，不能溢出或出现省略号。
- 选中单个单词时，选中文字以 `#3465D6` 下划线标识，并在所选词右上角显示删除按钮。
- 删除所选单词后，其原位置替换为可点击的空白词卡。
- 空白词卡会随填入内容的长度调整宽度；填充后背景与描边随机使用四种用户强调色之一。
- 当光标位于句尾时，显示“添加词卡”按钮；点击只在光标处插入空白词卡，不立即弹出面板。

## 7. 内容编辑底部面板

- 点击空白词卡时，页面显示遮罩和贴底内容编辑面板。
- 面板高度 289px，四周内边距统一为 20px，顶部与左右内容均按 20px 对齐。
- 面板背景使用 `sentence-add-dialog.svg`，顶部圆角、贴画板下边缘。
- 关闭按钮位于面板顶部和右侧各 20px 处。
- 模式切换使用 SVG 状态素材：Word、Photo、Audio；切换键顶部对齐。
- Word 模式提供词语输入、`⌘ Tab` 确认键、分类、搜索和推荐词。
- Photo 模式提供拍照、选图和最近图片网格。
- Audio 模式提供按住录音及最近音频列表。
- 超出面板可视范围的内容在面板内部纵向滚动，不改变面板位置或尺寸。

## 8. 资产命名

- 导航与页面素材：语义化英文名，例如 `back.svg`、`invite.svg`、`left-page.svg`。
- 用户头像：`user-1.svg` 至 `user-4.svg`。
- 用户标签：`user-label-1.svg` 至 `user-label-4.svg`。
- 内容模式状态：`content-tab-{word|photo|audio}-{default|selected}.svg`。

## 9. 验收重点

- 手机画板始终保持 375 × 812px 的比例与布局基准。
- 文本增多只导致句子卡向下扩展；头像和卡片宽度不变。
- 多张不同高度句子卡之间不重叠。
- 页面上限、翻页、卡片激活、词语删除、空白词卡填充和底部面板三种模式均可操作。
